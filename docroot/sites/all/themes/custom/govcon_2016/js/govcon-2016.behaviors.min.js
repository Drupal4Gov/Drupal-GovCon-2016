(function($) {
    Drupal.theme.prototype.govcon2016ExampleButton = function(path, title) {
        return $('<a href="' + path + '" title="' + title + '">' + title + "</a>");
    };
    Drupal.behaviors.govcon2016ExampleBehavior = {
        attach: function(context, settings) {
            $(".some-selector", context).once("foo", function() {
                var $anchor = Drupal.theme("govcon2016ExampleButton", settings.myExampleLinkPath, settings.myExampleLinkTitle);
                $anchor.appendTo(this);
            });
        }
    };
})(jQuery);